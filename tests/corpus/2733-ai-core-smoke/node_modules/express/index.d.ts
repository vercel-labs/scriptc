// Type surface for postures that keep the shipped declarations (island,
// flagless analysis). Static compilation drops these and types the bodies
// by inference instead.
declare interface Application {
  get(path: string, reply: () => string): void;
  handle(target: string): string | null;
  resolve(file: string): string | null;
}
declare function createApplication(): Application;

export default createApplication;
